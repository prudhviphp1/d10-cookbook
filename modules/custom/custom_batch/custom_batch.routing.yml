batch.process:
  path: '/admin/config/batch-form'
  defaults:
    _form: '\Drupal\custom_batch\Form\CustomBatchProcess'
    _title: 'Configuration Form with batch process'
  requirements:
    _permission: 'access content'
  # This below is ONLY for who has admin access
  options:
    _admin_route: TRUE
