<?php

/**
 * Implements hook_theme().
 */

function custom_twig_template_theme($existing, $type, $theme, $path) {

     return [
        'custom_twig_template' => [
            'variables' => ['text' => ""],
        ],
    ];
}
